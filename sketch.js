var FNameList, LNameList;
var FName, LName;
var StartPts;


function setup(){
  Width = screen.width;
  Height = screen.height;
  createCanvas(Width, Height);
}

function draw(){
  background(220);

}